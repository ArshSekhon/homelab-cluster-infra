---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: data-system
resources:
  - namespace.yaml
  - helmrelease.yaml
  - cluster.yaml
  # Include scheduled-backup.yaml only when a backup target is configured.
  # Remove or comment out the line below if no off-cluster S3 endpoint is set.
  - scheduled-backup.yaml
