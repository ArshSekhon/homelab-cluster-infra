---
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: data-services
  namespace: flux-system
spec:
  interval: 10m
  sourceRef:
    kind: GitRepository
    name: flux-system
  path: gitops/data-services/
  prune: true
  wait: true
  dependsOn:
    - name: storage
  decryption:
    provider: sops
    secretRef:
      name: sops-age
