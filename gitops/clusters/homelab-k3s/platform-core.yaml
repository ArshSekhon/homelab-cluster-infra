---
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: platform-core
  namespace: flux-system
spec:
  interval: 10m
  sourceRef:
    kind: GitRepository
    name: flux-system
  path: gitops/infrastructure/
  prune: true
  wait: true
  dependsOn:
    - name: platform-crds
  decryption:
    provider: sops
    secretRef:
      name: sops-age
