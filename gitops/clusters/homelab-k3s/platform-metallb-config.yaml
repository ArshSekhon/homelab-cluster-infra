---
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: platform-metallb-config
  namespace: flux-system
spec:
  interval: 10m
  sourceRef:
    kind: GitRepository
    name: flux-system
  path: gitops/infrastructure/metallb-config
  prune: true
  wait: true
  dependsOn:
    - name: platform-core
  decryption:
    provider: sops
    secretRef:
      name: sops-age
