---
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: platform-observability-config
  namespace: flux-system
spec:
  interval: 10m
  sourceRef:
    kind: GitRepository
    name: flux-system
  path: gitops/infrastructure/observability-config
  prune: true
  wait: true
  dependsOn:
    - name: observability
  decryption:
    provider: sops
    secretRef:
      name: sops-age
